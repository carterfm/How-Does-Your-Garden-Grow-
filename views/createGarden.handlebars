<main>
    <div class="welcome-box">
        <h2>Create a New Garden</h2>
        <p>Answer the questions below, and we'll help you plan a garden!</p>
        <form id="newGarden" style="display:flex;flex-direction:column; padding: 0 20px 20px 20px;">
            <label for="newTitle">What would you like to call this plan?</label>
            <input type="text" name="newTitle" id="newTitle" placeholder="My Plan's Name is...">

            <label for="newDesc">Any special notes?</label>
            <textarea name="newDesc" id="newDesc" cols="30" rows="5" placeholder="This plan's for..."></textarea>

            <label for="newShape">What shape is your garden bed?</label>
            <select name="newShape" id="newShape">
                <option value="">--Choose a Shape--</option>
                <option value="circle">Circle</option>
                <option value="square">Square</option>
                <option value="rectangle">Rectangle</option>
            </select>
            
            <div id="measQues">
                <label id="square">How long is one side (in inches)?</label>
                <label id="circle">How wide across is it (in inches)?</label>
                <label id="rectLong">How long is the long side (in inches)?</label>
                <input type="text" id="len" placeholder="Inches">
                <label id="rectShort">How long is the short side (in inches)?</label>
                <input type="text" id="wid" placeholder="Inches">
            </div>

            <label for="sun">How much sun do you get?</label>
            <select name="sun" id="newSun">
                <option value="">--Choose One--</option>
                <option value="full">Full sun (more than 6 hours of sun a day)</option>
                <option value="partial">Part sun (between 4-6 hours of sun a day)</option>
                <option value="shade">Full shade (less than 4 hours of sun a day)</option>
            </select>

            <div id="plantChoices" style="border: 1px solid black; margin:10px 0; padding: 0 10px;">
                <h3>Which plant would you like to include?</h3>
                <ul id="plantBox" style="margin: 10px; padding: 10px; width: 100%; height: 150px; overflow-y: auto">
                    {{#each plant}}
                        <li class="plantLI">
                            <input type="checkbox" class="plantChoice" name="{{id}}" value="{{id}}">
                            <img src="/images/{{name}}.png" alt="">
                            <div><label for="{{id}}">{{name}}</label><p>{{description}}</p></div>
                        </li>
                    {{/each}}
                </ul>
            </div>

            <button id="getGarden">Build It!</button>

        </form>
    </div>
</main>
<script src="/script/gardenForms.js"></script>